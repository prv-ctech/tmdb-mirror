# Compatibility entry point for the canonical four-container deployment.
# Edit deploy/compose.compact.yaml for host bind mounts and use
# deploy/env.production.example for behavior settings.
include:
  - path: ./deploy/compose.production.yaml
