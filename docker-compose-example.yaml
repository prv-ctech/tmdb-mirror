# Compatibility entry point for older checkout commands.
# The canonical four-service definition lives in deploy/compose.production.yaml.
name: tmdb

include:
  - path: ./deploy/compose.production.yaml
